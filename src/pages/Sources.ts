import {getManifest,getSources} from '../data/repository.js';
export async function sourcesPage(){const [m,s]=await Promise.all([getManifest(),getSources()]);const rows=Object.entries(s).map(([id,x])=>`<tr><td>${id}</td><td><a href="${x.url}" target="_blank">${x.title}</a></td><td>${x.type}<div class="small">${x.verification_status||''}</div></td><td>${x.note}</td></tr>`).join('');return `<div class="section"><h1>Source Library</h1><div class="callout green"><b>Verification date:</b> ${m.meta.as_of}</div><table class="tbl"><tr><th>ID</th><th>מקור</th><th>סוג</th><th>למה משתמשים בו</th></tr>${rows}</table></div>`}
