{
  "name": "wave-capital-crypto-analyst-university",
  "version": "12.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:content": "node scripts/validate-content.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "npm run validate:content && tsc -p tsconfig.build.json",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate:content && npm run typecheck && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
